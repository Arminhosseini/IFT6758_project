# docker-compose.yml
version: '3'

services:
  serving:
    build:
      context: ./
      dockerfile: ./Dockerfile.serving
      args:
        ARG: SERVING_PORT=5000
    image: my-flask-app
    ports:
     - 5000:5000
    networks:
      - mynetwork

  streamlit:
    build: 
      context: ./
      dockerfile: ./Dockerfile.streamlit
    ports:
      - 8501:8501
    depends_on:
      - serving
    image: streamlit-app
    networks:
      - mynetwork

networks:
  mynetwork: